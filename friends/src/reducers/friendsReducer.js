
  const initialState = {
    characters: [],
    // Array characters, Boolean fetching, null error.
  };
  
  export const reduceFriends = (state = initialState, action) => {
    switch (action.type) {
    }
  };